{
  "name": "exotic-touch-booking-zen",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix next-app run dev",
    "build": "npm --prefix next-app run build",
    "start": "npm --prefix next-app run start",
    "lint": "npm --prefix next-app run lint"
  }
}
